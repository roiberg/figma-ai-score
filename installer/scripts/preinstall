#!/bin/bash
# Pre-install: ensure the staging dir exists.
# macOS drops the pkg payload into /tmp/figma-ai-score-pkg, then
# postinstall copies the CLI files into the user's home directory.

set -e
mkdir -p /tmp/figma-ai-score-pkg
